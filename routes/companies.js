// const express = require("express");
// const routes = express.Router();
// const { isAdminAuth } = require("../middleware/admin");
// const { getAllCompanies, getAllOrders } = require("../controller/companies");

// routes.get("/get-all", isAdminAuth, getAllCompanies);
// routes.get("/orders/get-all", isAdminAuth, getAllOrders);

// module.exports = routes;
