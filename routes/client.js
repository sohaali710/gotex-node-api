// const express = require("express");
// const routes = express.Router();
// const clientControllers = require("../controller/clients");

// routes.post("/add-new-clint", isAuth, isMarketer, clientControllers.addNewClint);
// routes.post("/add-clint-deposit", isAuth, isMarketer, clientControllers.addDeposit);
// routes.get("/all-marketer-clint", isAuth, isMarketer, clientControllers.getAllMarketerClint);

// module.exports = routes;